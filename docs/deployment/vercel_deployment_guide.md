# Vercel Deployment Guide for NHAA-Care Frontend

1. Ensure Vercel CLI is authenticated:
   `npx vercel whoami`
2. Deploy directly:
   `npx vercel --prod`
3. Configure environment variable:
   `NEXT_PUBLIC_API_URL=https://your-backend-domain.gov.in/api/v1`
